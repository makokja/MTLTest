apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-go-app-chart.fullname" . }}
  labels:
    {{- include "test-go-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-go-app-chart.selectorLabels" . | nindent 4 }}
